spring.application.name=patient
server.port=8080
# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/patients-db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Glsid@2025

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
spring.jpa.show-sql=true

# Uncomment below lines if you want to use H2 for testing instead
# spring.datasource.url=jdbc:h2:mem:patient-db
# spring.h2.console.enabled=true